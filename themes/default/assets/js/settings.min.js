$(function () {
    
    setTimeout(function(){
        $('#m-settings').trigger('click');
    },650);

    //  form profile update User

    $('body').on('submit', '.form-settings', function (e) {
        e.preventDefault();
        var form = new FormData(this);
        $.ajax({
            url: SITEURL + "/dashboard/updateUser",
            dataType: 'json',
            type: 'POST',
            data: form,
            processData: false,
            contentType: false,
            beforeSend: function () {
                $(".preload").fadeIn(300);
            },
            success: function (result) {
                $(".preload").fadeOut(300);
                toastr[result.status](result.msg);
                if (result.status == "success") {
                    $('.clear').val('');
                    $('.user-name').html(result.model.name);
                    $('.img-user').attr('src', result.model.image);
                }
            }
        });
    });
    
});


